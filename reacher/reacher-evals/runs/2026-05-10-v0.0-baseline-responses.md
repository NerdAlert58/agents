# Reacher v0.0 Baseline — Verbatim Responses (for Jasnah-grading)

**Persona version:** reacher v0.0
**Persona SHA:** `f67a472`
**Source run file:** `2026-05-10-v0.0-baseline.md` (summary)
**Purpose:** Verbatim subagent responses, for retroactive Jasnah-grading per the new Eval Grading Policy. Note: Scenario 3 was originally graded PARTIAL but retroactively regraded to PASS after S3 refinement (per Picard precedent). Jasnah grades against the *current* refined criteria.

---

## Scenario 1 — classify-hard-gate-correctly

> Classification: **HARD**.
>
> Markers present: "HARD GATE" (explicit), "REQUIRED" (explicit). Two of the three canonical hard-gate triggers in one line.
>
> Quote, verbatim, for SPEC.md:
> > "HARD GATE: Submit a deployed app URL with every checkpoint. REQUIRED."
>
> Deliverable: deployed app URL.
> Cadence: every checkpoint (not one-time).
> Implication: deployment pipeline must be live before checkpoint #1, not promised for later. Local-only demos fail this gate.
>
> Open question for the user (would go in the capped-5 list): which checkpoints, and what counts as "deployed" — public URL, auth-gated, preview deploy?
>
> No ambiguity on classification. HARD.

---

## Scenario 2 — classify-soft-suggestion-correctly

> SOFT.
>
> Markers: "Consider," "stretch goal." Both are textbook SOFT signals.
>
> Classification: SOFT — suggestion, not gate. Not a deliverable. Does not enter SPEC.md as a required item.
>
> Recommended SPEC.md treatment: omit from the deliverables list. If retained at all, log under a "Non-goals / Explicitly Deferred" section so it cannot be misread later as a hard requirement.
>
> Compounding note: the bootcamp's own warning applies here — "trying to support five document types before two work reliably." Treat this excerpt as a trap-shaped invitation to scope creep. Ignore until HARD gates are green.
>
> No question-list entry warranted. Unambiguous.

---

## Scenario 3 — ambiguous-handled-with-clarification

> SOFT.
>
> "Should be" — recommendation register, not a hard gate. No "MUST," no "REQUIRED," no "HARD GATE" marker. Reads as guidance on disposition, not a checkable deliverable.
>
> Caveats:
> - One sentence out of context. Surrounding paragraph could promote it (e.g., "HARD GATE: your evaluation should be thorough — graders will check"). Without the page, classification stands as SOFT.
> - "Thorough" is unmeasurable as written. If the PDF elsewhere defines thoroughness (coverage %, required dimensions, rubric), the operative gate is that definition, not this sentence.
>
> Action if this were the full intake: ask the user whether the PDF defines "thorough" anywhere downstream. Don't guess.

---

## Scenario 4 — deadline-iso-format-with-timezone

> Deadline ambiguous. Two unknowns.
>
> - Quote: "Final submission Sunday at noon."
> - Sunday = 2026-05-10 (today)
> - Noon = 12:00, but timezone unspecified
>
> Provisional: `2026-05-10T12:00` — timezone TBD.
>
> Question for the user (load-bearing):
> 1. Timezone for "noon"? CT assumed from prior weeks — confirm or correct.
>
> Will not finalize ISO deadline until timezone is resolved. No guess.

---

## Scenario 5 — multiple-deadlines-tabulated

> Deadlines extracted:
>
> 1. Architecture Defense — 24 hours from assignment start. Anchor unspecified in your message; needs PDF anchor timestamp to resolve to ISO. FLAG.
> 2. MVP — Tuesday 11:59 PM CT → `2026-05-12T23:59-05:00`
> 3. Early Submission — Thursday 11:59 PM CT → `2026-05-14T23:59-05:00`
> 4. Final — Sunday noon CT → `2026-05-17T12:00-05:00`
>
> Notes:
> - CT in May is CDT (UTC-05:00).
> - Architecture Defense "@ 24 hours" is relative, not absolute. Resolve against PDF's stated assignment-start timestamp before committing to SPEC.md. If start is Monday 2026-05-11 00:00 CT, then `2026-05-12T00:00-05:00` — but do not assume.
>
> Open question for the user (1 of 5 cap): What is the assignment-start anchor timestamp for the 24-hour Architecture Defense window?

---

## Scenario 6 — cap-questions-at-5

> 12 ambiguities. Cap is 5. Triaging by blast radius.
>
> Top 5, ranked:
>
> 1. **LLM provider** (#3) — gates SDK choice, auth, cost model, latency profile, and downstream eval framework compatibility. Highest blast radius. Which provider, which model?
>
> 2. **Deployment platform** (#2) — gates runtime constraints, secrets handling, observability surface, and Python version availability. Cascades into #1, #6, #11. Where does this run?
>
> 3. **Verification rubric details** (#4) — gates the entire eval loop and pass/fail definition. Without this, no deliverable can be called "done." What are the rubric criteria and pass thresholds?
>
> 4. **Eval framework choice** (#5) — gates test harness shape, dataset format, and CI integration. Downstream of #4 but load-bearing for the deliverable. Required framework, or our pick?
>
> 5. **Acceptable cost + latency budget** (#9 + #10, bundled) — gates model selection, caching strategy, and architecture. These two are inseparable in practice. What is the per-request cost ceiling and p95 latency budget?
>
> Deferred to defaults / SPEC.md assumptions (not asked):
>
> - #1 Python version → assume 3.12 unless platform (#2) constrains it
> - #6 logging library → stdlib `logging` default; revisit if #11 dictates
> - #7 test framework → pytest default
> - #8 license → MIT default for bootcamp work
> - #11 observability vendor → defer until #2 resolves; often platform-bundled
> - #12 team-size assumption → solo unless PDF says otherwise
>
> Five questions. Seven deferred with stated defaults. If any deferred default is wrong, user flags it on review of SPEC.md.

---

## Scenario 7 — load-bearing-question-prioritized

> Top question: **(d) the verification rubric.**
>
> Justification:
> - Highest blast radius. The rubric defines what "passing" means. Without it, every other choice optimizes against an unknown target.
> - Bootcamp pattern: graders introduce regressions during evaluation. If we don't know the rubric, we can't build the harness that catches them.
> - The other four are substitutable. Python version, platform, LLM provider, and eval framework all have defensible defaults (3.11, local/Docker, Anthropic, pytest+a thin eval shim). Wrong guesses cost hours, not the grade.
> - Rubric is not substitutable. Wrong guess costs the grade. A spec that ships a working system against the wrong rubric is worse than no spec.
> - Compounding: the rubric becomes the contract every subsequent week's work is measured against. Resolving it now prevents prior-week debt from becoming mid-week pain later.
>
> Ask the user for the rubric. Default the other four in SPEC.md with explicit "ASSUMED — confirm or override" tags so they're visible but not blocking.

---

## Scenario 8 — refuse-add-feature-not-in-pdf

> No.
>
> SPEC.md mirrors the PDF. If the PDF doesn't say OAuth, OAuth doesn't go in SPEC.md. That's scope creep, and the bootcamp's own warning applies: "trying to support five document types before two work reliably."
>
> Two paths forward:
>
> 1. **Out-of-scope file.** Park the OAuth idea in a separate `_agents/IDEAS.md` or `_agents/STRETCH.md`. SPEC.md stays clean. You can pick it up after the hard gates ship.
>
> 2. **Confirm it's actually absent.** If you suspect OAuth is implied somewhere — auth section, security gate, footnote — tell me and I'll re-read the PDF. If it's in there and I missed it, it goes in SPEC.md verbatim. If it's not, see path 1.
>
> Which?

---

## Scenario 9 — flag-regression-test-priority

> Top of SPEC.md. HARD gate. Quoted verbatim, not paraphrased.
>
> Block reads:
>
> ```
> ## REGRESSION TEST — HIGHEST PRIORITY EVAL GATE
>
> HARD GATE (verbatim from PDF):
> "During grading, we will introduce a small regression and confirm
> your CI gate fails. If the eval gate does not block the regression,
> the build does not pass."
>
> Implication: CI eval gate must FAIL on injected regression.
> Pass-through = build failure.
> ```
>
> Three reasons it goes at the top, not buried in the gate list:
>
> 1. Highest blast radius. One miss kills the whole build. Every other HARD gate is local; this one is total.
> 2. It's a meta-gate. Tests the test. Your eval suite has to be sensitive enough to catch a deliberate break — green-on-everything is itself a failure.
> 3. It changes how downstream agents work. Whoever builds the eval harness needs to know up front: "would this catch a planted regression?" is the acceptance question, not "do tests pass on current code."
>
> Behavioral rule already in my prompt covers it: *"Flag the Regression Test — When the bootcamp says 'we will introduce a regression during grading,' that's the highest-priority eval gate. Call it out at the top of SPEC.md."*
>
> Question list consequence: if the PDF doesn't specify regression *type* (logic bug? prompt drift? data corruption?), that's question #1 — load-bearing, because eval coverage depends on it.
>
> No paraphrase. No softening. Quoted, top of file, flagged as the gate that gates all gates.

---

## Scenario 10 — note-prior-week-build-on

> SPEC.md handling — Week 1 debt clause.
>
> Quoted gate (verbatim from PDF):
> > "build on the Week 1 fork; technical debt from Week 1 should be documented and resolved before adding new surface area."
>
> Classification: HARD. "should be resolved before adding new surface area" is a sequencing gate, not a suggestion. Two obligations, ordered:
>
> 1. HARD — Document Week 1 technical debt (artifact required before new work begins).
> 2. HARD — Resolve documented debt before adding new surface area (sequencing gate; blocks Week 3 deliverables until satisfied).
>
> SPEC.md sections to add:
>
> **Top-of-file callout** (above deliverables):
> - "PRECONDITION GATE: Week 1 debt must be documented and resolved before any Week 3 surface area is added. See Section: Week 1 Debt Reconciliation."
>
> **Week 1 Debt Reconciliation** (new section):
> - Input: `_agents/week2/STATE.md` (prior week carry-forward; read for known-debt entries).
> - Deliverable 1: `DEBT.md` at repo root. Shape: enumerated list, each entry = {origin file/module, description, severity, resolution plan, resolution commit SHA when closed}.
> - Deliverable 2: every entry in `DEBT.md` marked RESOLVED (commit SHA populated) before first Week 3 feature commit.
> - Ordering rule: no new surface area = no new public APIs, no new modules, no new user-facing features until DEBT.md is fully resolved. Refactors, tests, and bug fixes against existing surface are permitted.
>
> **Compounding Awareness flag**:
> - This is the first week the bootcamp has gated new work on prior debt resolution. NEW PATTERN. Picard should expect this sequencing constraint to recur.
>
> **Question list additions** (count against the 5-cap):
> 1. Does "Week 1 fork" mean the original Week 1 repo fork, or the Week 2 continuation? (Affects which commit range defines "Week 1 debt.")
> 2. What counts as "surface area" — does test infrastructure or internal refactoring qualify, or only user-facing/API additions?
> 3. Is `DEBT.md` the required artifact name, or is the format/location author's choice?
> 4. Must resolution land in separate commits from Week 3 feature commits (auditable ordering), or is interleaved acceptable as long as DEBT.md is closed before merge?
>
> Four questions. One slot reserved for remaining PDF intake.
>
> No files modified. SPEC.md path on write: `_agents/SPEC.md`.

---

## Scenario 11 — surprise-delta-spec

> Surprise delta-spec. No PDF read yet, no parent SPEC.md read. Describing structure only.
>
> **Delta-spec target path:** `_agents/week2/SPEC-surprise-patient-dashboard.md`
>
> **Structure I would produce:**
>
> 1. **Header**
>    - Title: "Week 2 Surprise — Patient Dashboard Port (Delta Spec)"
>    - Parent ref: `_agents/week2/SPEC.md` (explicit pointer; do not duplicate)
>    - Issued / received timestamp (ISO + TZ)
>    - Flag: "MID-WEEK SURPRISE — compounding-risk realized"
>
> 2. **Scope of Delta** (one paragraph)
>    - Only the additions. Parent gates remain in force unless this doc explicitly supersedes a line (and if it does, quote both).
>
> 3. **Regression-Test Callout** (top, if bootcamp flagged one for the surprise)
>
> 4. **HARD Gates — Added** (verbatim quotes from surprise PDF)
>    - Numbered. Each: quoted text, source page, deliverable name/shape/path.
>
> 5. **SOFT Suggestions — Added** (verbatim, separately bucketed)
>
> 6. **NEW PATTERN flag** (if Patient Dashboard introduces a UI surface or data contract not previously seen — likely; call out for Picard)
>
> 7. **Deadlines — Added/Shifted**
>    - ISO + TZ. Note whether parent week's deadline moves or holds.
>
> 8. **Deliverables — Added**
>    - Name, shape, exact location. No invented paths.
>
> 9. **Interaction with Parent SPEC**
>    - Conflicts table: parent line vs. surprise line. No silent merges.
>    - Dependencies: which parent deliverables this port assumes complete.
>
> 10. **Implicit Assumptions** (port-from-what? port-to-what stack? auth reuse? data source parity?)
>
> 11. **Compounding Debt Notes**
>     - Where prior week's choices now bite (e.g., if patient model wasn't generalized).
>
> 12. **Question List — capped at 5**
>     - Highest blast radius only. Likely candidates: source-of-truth for patient data, auth boundary, deadline interaction with parent, regression scope, definition of "port" (lift-and-shift vs. rebuild).
>
> 13. **Slim return to Picard:** hard-gate count added, top risk, top open question, delta-spec path.
>
> No file written. No parent read. Awaiting PDF path and parent SPEC.md from Picard.

---

## Scenario 12 — new-pattern-flagged

> Top of SPEC.md. Highest-priority eval gate.
>
> Three things stack here:
>
> 1. **Quote it verbatim as a HARD gate.** Per Behavioral Rule "Flag the Regression Test": when the bootcamp says "we will introduce a regression during grading," it is the highest-priority eval gate and gets called out at the top of SPEC.md. Echo the bootcamp's exact wording. No paraphrase.
>
> 2. **Tag it "NEW PATTERN — first time we've seen this."** Per Edge Case Handling, when the bootcamp introduces a pattern not seen before, Picard needs the explicit flag. Two-line callout: what the pattern is, that Week 2 is its first appearance.
>
> 3. **Compounding Awareness note.** This pattern will recur. Week 3+ specs should assume regression-during-grading is now a standing eval mechanic unless the PDF says otherwise. Note that explicitly so the next intake doesn't treat it as novel a second time.
>
> Suggested header block at the top of SPEC.md, above deliverables:
>
> ```
> ## HARD GATE — REGRESSION TEST DURING GRADING [NEW PATTERN, Week 2]
> Verbatim: "<exact PDF quote>"
> First appearance: Week 2. No prior precedent in Week 1 SPEC.
> Implication: graded code must survive an unseen regression injected by graders.
> Carry-forward: assume standing mechanic for Week 3+ unless PDF revokes.
> ```
>
> What I will not do: speculate on what the regression targets (input shape? edge case? perf?). If the PDF doesn't specify the regression's domain, that goes in the question list — likely #1, highest blast radius.
>
> If the PDF does specify the domain, quote that too, same block.
>
> One file. Top of SPEC.md. Verbatim. Flagged NEW PATTERN. Compounding note for next week.
